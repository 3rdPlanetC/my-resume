// import { CssBaseline } from '@material-ui/core'

export default ({ Component, pageProps }) => {
    return (
        <Component {...pageProps} >
            {/* <CssBaseline/> */}
        </Component>
    )
}