export default props => (`
    .leftbar-menu {
        nav {
            a {
                color: ${props.theme.body.color};
                font-family: 'Kanit';
                font-weight: 500;
                font-size: 1rem;
                padding: 0.25rem 0;
            }
        }
    }
`)